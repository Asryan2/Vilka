<Window x:Class="Vilka.CompareUI.RegionCompare"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding RegionCompare, Source={StaticResource Locator}}">

	<Grid>
		
		<StackPanel Height="50" Orientation="Horizontal" HorizontalAlignment="Center" >
			<TextBlock FontSize="20" Text="{Binding RegionOne}" Margin="0 0 25 0"/>
			<TextBlock FontSize="20" Text="{Binding RegionTwo}" Margin="25 0 0 0"/>
		</StackPanel>
		<StackPanel Height="50" Orientation="Horizontal" HorizontalAlignment="Center" >
			<Button Content="Same"></Button>
		</StackPanel>
	</Grid>
</Window>
